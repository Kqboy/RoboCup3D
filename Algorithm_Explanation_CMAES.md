# CMA-ES 算法及其在参数优化中的应用

## 1. CMA-ES 算法简介 (Covariance Matrix Adaptation Evolution Strategy)

CMA-ES（协方差矩阵自适应进化策略）是一种用于解决非线性、非凸优化问题的进化算法。它特别适用于连续域上的黑盒优化问题，即我们不知道目标函数的具体形式（无梯度），只能通过输入参数获得输出结果。

### 核心原理
CMA-ES 维护一个多元正态分布（Gaussian Distribution）来模拟解的分布。每一代迭代过程如下：
1.  **采样 (Sampling)**: 根据当前的均值和协方差矩阵，生成一组候选解（种群）。
2.  **评估 (Evaluation)**: 计算每个候选解的适应度（Fitness/Loss）。
3.  **选择与更新 (Selection & Update)**:
    *   **均值更新**: 选择表现最好的部分解，加权平均计算新的中心（均值）。
    *   **协方差矩阵更新**: 根据由于选择操作导致的分布变化，调整协方差矩阵。这使得算法能够学习参数之间的相关性（例如，参数 A 变大时参数 B 需要变小）。
    *   **步长控制**: 调整搜索的范围（步长）。

### 优势
*   **无梯度**: 不需要计算梯度，适合仿真、物理实验等不可导场景。
*   **自适应**: 能够自动调整搜索方向和步长，收敛速度快。
*   **抗局部最优**: 具有一定的全局搜索能力。

## 2. 本项目中的 CMA-ES 应用

在本项目中，CMA-ES 被用于微调机器人的**踢球前的站位和朝向**参数。代码位于 `优化_cma_train_t长踢的定位和bias.py`。

### 2.1 优化目标
我们希望找到一组参数，使得机器人踢出的球：
1.  **距离远**: 尽可能接近或超过 20 米。
2.  **方向准**: 尽可能沿直线（Y=0）飞行。

### 2.2 待优化参数 (Search Space)
优化器主要调整以下 3 个参数：
1.  **Ball Limit X**: 机器人相对于球的 X 轴定位偏移。
2.  **Ball Limit Y**: 机器人相对于球的 Y 轴定位偏移。
3.  **Kick Bias Dir**: 踢球时的朝向偏差补偿（用于修正机器人自身的机械偏差）。

在代码中，这些参数被编码为一个向量 `param`。

### 2.3 适应度函数 (Loss Function)
为了评估一组参数的好坏，定义了 `loss()` 函数：

1.  **多点测试**:
    为了避免偶然性，对每一组参数，机器人会进行 **4 次** 踢球测试，分别对应不同的球初始位置（左、中、右）。

2.  **单次损失计算**:
    $$ Loss_{single} = 0.6 \times (20 - x_{final})^2 + 0.4 \times (3 \times y_{final})^2 $$
    *   **X轴项**: `(20 - x)^2`。目标是 X=20。权重 0.6。
    *   **Y轴项**: `(3 * y)^2`。目标是 Y=0。权重 0.4，且对 Y 偏差极其敏感（乘了 3 倍）。这意味着侧向偏离会被重罚。

3.  **最终损失聚合**:
    *   对 4 次测试的 Loss 进行排序。
    *   取 **最低的 2 个 Loss 的平均值** 作为该组参数的最终 Score。
    *   *设计意图*: 这种“取最好”的策略可能是为了容忍仿真中的随机噪声（如意外摔倒、物理引擎抖动），只关注该参数下“表现好”的那些样本。

### 2.4 优化流程
1.  **初始化**:
    *   从 `optimizer_state.pkl` 加载（断点续传）或重新初始化 `cmaes.CMA`。
    *   设定初始均值 (`init_param`) 和搜索范围 (`bounds`)。
    *   种群大小 (`population_size`) 设为 20。

2.  **迭代循环**:
    *   `optimizer.ask()`: 算法提出一组新参数。
    *   **仿真执行**:
        *   `slot.set_ball_limits_fixed_distance(...)`: 将参数应用到机器人的动作槽引擎中。
        *   `K.kick_once()`: 在仿真环境中执行踢球，测量落点。
    *   `optimizer.tell()`: 将参数及其对应的 Loss 反馈给算法。
    *   **记录**: 保存每一代的最优结果到 `result.txt`。

### 2.5 总结
这是一个典型的 **Sim-to-Real** (或 Sim-to-Sim) 参数调优案例。
*   PPO 负责学习复杂的**关节运动控制**（高频、高维）。
*   CMA-ES 负责学习低频的、全局的**策略参数**（站位、朝向）。
两者结合，既保证了动作的灵活性，又保证了宏观策略的有效性。
