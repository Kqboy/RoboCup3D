# PPO 算法及其在踢球训练中的应用

## 1. PPO 算法简介 (Proximal Policy Optimization)

PPO（近端策略优化）是 OpenAI 提出的一种强化学习算法，目前被认为是深度强化学习中的基准算法。它属于 Policy Gradient（策略梯度）家族，旨在解决传统策略梯度方法中更新步长难以确定的问题。

### 核心思想
PPO 的核心在于限制策略更新的幅度，防止一次更新过大导致策略崩溃。它通过引入一个“裁剪（Clip）”机制来实现这一点：

*   **Surrogate Objective（代理目标函数）**: PPO 构造了一个特殊的目标函数，该函数在概率比率（新策略/旧策略）偏离 1 太远时会被截断（Clip）。
*   **稳定性**: 这种机制保证了新策略不会偏离旧策略太远，从而保证了训练的稳定性。
*   **数据效率**: 相比于 TRPO 等算法，PPO 实现更简单，且允许使用同一批数据进行多次小批量的更新（Mini-batch update）。

## 2. 本项目中的 PPO 应用

在本项目中，PPO 被用于训练机器人执行复杂的踢球动作。代码主要集中在 `kick.py` 文件中，利用 `stable_baselines3` 库实现。

### 2.1 环境定义 (`Kick` 类)
为了使用 PPO，项目定义了一个符合 OpenAI Gym 接口的自定义环境 `Kick`。

*   **状态空间 (Observation Space)**:
    机器人感知到的环境信息，包含约 70+ 个维度的连续特征：
    *   **基础状态**: 步数进度、头部高度/速度、躯干姿态（IMU）、角速度、加速度、足部压力。
    *   **关节状态**: 全身 20-22 个关节的当前角度和角速度。
    *   **球的信息**: 球相对于机器人的位置、速度、距离，以及目标踢球方向的偏差。
    *   *代码参考*: `Kick.observe` 方法。

*   **动作空间 (Action Space)**:
    机器人可以控制的动作。
    *   这是一个连续空间，维度对应机器人的关节数量（20 或 22）。
    *   PPO 输出的值被视为关节角度的**偏移量**。
    *   *平滑处理*: 代码中使用了指数移动平均（EMA）来平滑动作序列 (`self.act = 0.4 * self.act + 0.6 * action`)，避免动作抖动。
    *   *叠加控制*: 最终关节角度 = 基础动作模板（如 `Kick_Motion` 插值）+ PPO 输出的偏移量。这是一种**残差学习**（Residual Learning）的思路，即让 RL 只需要学习如何在现有动作基础上进行微调，而不是从零学习动作。

### 2.2 奖励函数 (Reward Function)
奖励函数决定了 AI 学习的目标。在 `Kick.step` 方法中定义：

$$ Reward = 1.5 \times Distance - 10 \times |Height - 0.4| + Fall\_Penalty $$

*   **`1.5 * dis` (距离奖励)**: 鼓励机器人将球踢得越远越好。
*   **`-10 * abs(high - 0.4)` (高度惩罚)**: 控制踢球的高度。这里似乎希望球保持在 0.4m 左右的高度（或者是惩罚过高的球，具体取决于 `high` 的物理含义，通常是指球飞行过程中的最大高度）。
*   **`fall` (摔倒惩罚)**: 如果机器人摔倒，给予负奖励（代码中被注释掉了或逻辑较复杂，但在 `fall` 变量初始化为 0）。

### 2.3 训练配置 (`Train` 类)
*   **算法实现**: `stable_baselines3.PPO`
*   **并行环境**: 使用 `SubprocVecEnv` 同时运行多个仿真实例（默认为 CPU 核心数，最多 16 个），极大地加速了采样过程。
*   **关键超参数**:
    *   `n_steps`: 256 (每次更新前每个环境采样的步数)
    *   `batch_size`: 64
    *   `learning_rate`: 3e-4
    *   `clip_range`: 0.2 (PPO 的核心裁剪参数)
    *   `total_steps`: 5,000,000 (总训练步数)

### 2.4 总结
本项目采用了一种**分层/残差控制**的策略：
1.  **底层**: 使用预定义的关键帧动画（Slot/Kick_Motion）提供基本的踢球动作。
2.  **上层 (PPO)**: 训练一个神经网络策略，根据当前状态（球的位置、自身姿态）动态调整关节角度的偏移量。

这种方法比纯端到端（End-to-End）学习更容易收敛，因为它结合了先验知识（动作模板）和 RL 的适应能力。
